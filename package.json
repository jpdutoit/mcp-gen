{
  "name": "mcp-gen",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "mcp-gen": "./dist/mcp-gen.js"
  },
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "esbuild --bundle --platform=node --target=node24 --format=esm --outfile=dist/mcp-gen.js src/cli.ts --sourcemap --minify --external:esbuild --banner:js=\"import { createRequire as _cr } from 'module'; import { fileURLToPath as _fu } from 'url'; import { dirname as _dn } from 'path'; const require = _cr(import.meta.url); const __filename = _fu(import.meta.url); const __dirname = _dn(__filename);\"",
    "dev": "tsc --watch",
    "inspect": "node scripts/inspect.js",
    "clean": "rm -rf examples/*/output examples/*/*-output",
    "build:examples": "node scripts/build-examples.js"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "cli",
    "typescript"
  ],
  "author": "jpdutoit",
  "license": "ISC",
  "packageManager": "pnpm@10.27.0",
  "description": "Generate MCP servers from TypeScript files",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.2",
    "esbuild": "^0.27.2",
    "ts-morph": "^27.0.2",
    "typescript": "^5.9.3",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "commander": "^14.0.2",
    "@modelcontextprotocol/inspector": "^0.18.0",
    "@types/node": "^25.0.8",
    "mcp-server-tester": "^1.4.1"
  }
}
